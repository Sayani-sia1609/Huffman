#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <map>

std::map<char,int> calcfreq(const std::string& str);

#endif // UTILS_H
#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <map>

using namespace std;

map<char,int> calcfreq(string input_string);

#endif